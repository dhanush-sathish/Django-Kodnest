# Django-Kodnest

A Django-based project for Kodnest.

## Overview

This repository contains the Django-Kodnest application, set up for orchestrated development with automated issue tracking and webhook integrations.

## Getting Started

1. Clone the repository
2. Install dependencies: `pip install -r requirements.txt`
3. Run migrations: `python manage.py migrate`
4. Start the server: `python manage.py runserver`

## Contributing

Please follow the conventional commit format (`feat:`, `fix:`, `chore:`, etc.) when submitting changes.
